<li>
	{{burger_name}}

	<button class="change-delicious" data-id="{{id}}" data-newdelicious="{{delicious}}">
		{{#if delicious}}Probably going to sit awhile...{{else}}DEVOUR IT!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
